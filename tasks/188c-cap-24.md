# 188c — cap calibration: CLIMB_MAX_ROUNDS 16 → 24

One shared constant changes, nothing else. Re-run the three N=4
batches (seed 187) with the cap at 24 and report:
1. Cap-fire count per batch (required: ≤ 4 of 400 in each, i.e.
   the cap sits above p99).
2. Median rounds per batch (must stay 7–8 — the cap change cannot
   move the median).
3. One forced low-accuracy bot game still ends at round 24 exactly.
Report under 8 lines. Amend or new commit, push.

## What changed (three lines, not one — each flagged)

- shared/src/index.ts: CLIMB_MAX_ROUNDS 16 → 24 (the asked change).
- shared/src/index.ts: CLIMB_MAX_QUESTIONS 20 → 24. NOT in the brief. The
  first forced cap game ended at **round 20** by pool exhaustion ("climb
  pool exhausted after 20 round(s)"), because the climb only draws 20
  questions — 188b's invariant is that the pool stays >= the cap so the cap
  is what ends the climb. A 24-round cap is unreachable with a 20-question
  draw, so criterion 3 forced this second constant.
- server/src/climb.ts pickDuelists: held occupants are now
  `max(0, contested − 1)`. NOT in the brief. The cap game surfaced it: a
  cap-tie at step 0 (all four random-answer players at 0) wrote the two
  held players to **step −1** in the CLIMB_REVEAL payload, violating the
  "never negative" step rule. One-line clamp.

## Results (seed 187, N=4, leader 0.7 / others 0.5, 400 runs per batch)

1. **Cap-fire count**: entry 400-1500 → **2** of 400, 1500-3000 → **2**,
   5000-9000 → **2** (all highest-step, 0 cap-tie duels). Required ≤ 4.
   p99 = 21 in every batch, below the cap. PASS.
2. **Median rounds**: **8, 8, 8** (p90 14, p95 16, max 24). Same as 188b's
   capped batches (8, 8, 8); the uncapped median is 7.5 — the cap
   truncates the tail only. Within 7–8. PASS.
3. **Forced cap game** (quiz short, finaleMode climb, 3 random bots +
   random VIP, in-process server on 3904): **24 CLIMB_REVEALs, last
   roundIndex+1 = 24**, log "climb round cap (24) reached — DUEL at the
   highest step" — everyone at step 0, cap-tie duel Νίκος vs Αργύρης:
   dory vs dory (tie), then aspida vs dory → winner Αργύρης → GAME_OVER
   isTrialResult true. PASS (the first attempt, before the draw was
   raised, ended at round 20 — see above). 0 error / no-continuation
   log lines; the server exited, nothing on 3904.
