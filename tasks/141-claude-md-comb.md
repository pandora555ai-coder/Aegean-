# Task 141 — CLAUDE.md milestone comb: tasks 132-140

Model: Sonnet.

The comb rule (117/124/131): every claim VERIFIED against the repo —
where the paper and the code disagree, the paper loses. Traps carry
file:line proof. CLAUDE.md is the agent's BRIEF (rules, file map,
traps), not history — deletions count as much as additions. The
design MDs are NOT in the repo; do not create or touch any of them.

Fold in, each claim verified before written:

- modes/full.ts: FOURTH registry mode composing quiz/draw/numeric/
  trial. Standalones untouched, VIP-selectable — the dev harness.
  Sub-segment ends route to the next STAGE_ANNOUNCE; exactly ONE
  GAME_OVER; scores carry; minPlayers 2; FULL_QUIZ_QUESTION_COUNTS
  short 2+2 / medium 3+3 / long 5+5; segment counts are CALL-SITE
  parameters (standalone numeric still asks 5).
- Scales: FULL_QUIZ_SCORE_SCALE + the draw guess scale put quiz and
  guess points at ~400 max IN FULL ONLY — parameters defaulting to
  1, never a mode check inside a scoring function. Steal and the
  drawer proportion INTENTIONALLY unscaled.
- Numeric: no answer scores 0, excluded from ranking; N =
  submitters. scoreNumericSubmissions is server-only
  (server/src/numeric.ts); the /dev tool does not import it.
- NEW TRAP (140), file:line: phase-scoped CLIENT state must be
  cleared on phase transitions — a stale numericReveal in
  ControllerScreen sat above question/trialQuestion in the render
  if-chain and masked every phone view from the numeric segment's
  end through the whole trial; cleared in handleQuestionShow.
  Corollary rule: BOTS ANSWER AT THE SOCKET LEVEL and never render
  a phone — phone-rendering bugs need a Playwright phone client or
  a human, never a bot run.
- TRAP DEEPENED (137), cite BOTH sites: the round that declares
  sudden death flags every duelist eliminated:true — winner
  included — so "read results[].eliminated, never score<=0" is no
  longer sufficient alone; the server's eliminationOrder and the
  client's row-removal scheduling must ALSO gate on
  nextSuddenDeath.
- Trial display: eliminated rows LEAVE the column ~2200ms after
  their reveal (after the counter+reorder tween) — REPLACES the 128
  stay-at-0.42 note if CLAUDE.md carries it. Trial GAME_OVER shows
  NO digits; standings are survival order.
- SOCRATES in draw/numeric: detection behind the LINE_TAGS gate
  (zero lines = detects, never fires). TRIAL_INTRO_LINES plays at
  the trial announce; the Συκοφαντία pool is aliased to stage 4 in
  full.
- AUDIO PROVENANCE rule: all audio — Socrates TTS and crowd SFX —
  is generated by Argyrios via ElevenLabs. Agents never attempt to
  generate audio; DSP post-processing of existing files is fine
  when a task asks for it.

## Acceptance criteria

Report on EACH one separately. Under 8 lines.

1. Count of file:line citations added, each checked live — and list
   any claim above that FAILED verification (write what the repo
   actually says instead; the paper loses).
2. Staleness sweep: count of existing CLAUDE.md statements the repo
   now contradicts (e.g. numeric no-answer 100, the 128 elimination
   display, "merge not built") — all fixed or deleted.
3. Diff shape (lines added/removed) and the FINAL line count — if it
   ended over ~220, deletions were missed.
4. Confirm zero files created outside CLAUDE.md and the task doc.
